import React from 'react';

interface PageProfileProps {
    // account: User;
}

const PageProfile: React.FC<PageProfileProps> = (props) => {
    return (
        <div>
            Welcome to the Profile Page.
        </div>
    )
}

export default PageProfile;
