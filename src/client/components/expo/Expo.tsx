import * as React from 'react';
// import './Expo.scss';

interface ExpoProps {}

const Expo: React.SFC<ExpoProps> = (props) => {
    return (
        <div>
            <h1>Expo Page Thing</h1>
        </div>
    );
};

export default Expo;
