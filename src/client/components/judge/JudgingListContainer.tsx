// const mapStateToProps = state => {
//     return {
//         username: state.username,
//     };
// };

// const mapDispatchToProps = dispatch => {

// };

// const mapStateToPermissions = state => {

// };

// return auth(connect(mapStateToProps, mapDispatchToProps, Component));